nelmio_cors:
    defaults:
        origin_regex: false # ВЫКЛЮЧАЕМ РЕГУЛЯРКИ, они тут только ломают всё
        allow_methods: ['GET', 'OPTIONS', 'POST', 'PUT', 'PATCH', 'DELETE']
        allow_headers: ['Content-Type', 'Authorization']
        expose_headers: ['Link']
        max_age: 3600
    paths: []